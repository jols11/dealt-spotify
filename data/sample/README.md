# Synthetic listening sample

This folder can hold exported, non-personal fixtures. The application generates
the demo library at runtime (`POST /api/auth/demo`) using the catalog in
`backend/app/services/demo_seed.py`.

Plays are invented. Artist and track *names* are well-known labels used only so
the interface feels realistic. Do not treat this as anyone’s Spotify history.
