# Static UI only. Deal / search / Spotify need the FastAPI process
# (see README: deploy the Dockerfile on Render, Railway, or Fly).
[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
